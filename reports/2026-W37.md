# Weekly Report — 2026-W37 (Sep 6–12)

**TL;DR:** The champion is unchanged (`mom_zscore_overlap6_hzn_avg4`, holdout Sharpe 1.292) — no holdout look was spent this week, all 8 trials ran on the scout track. The week's headline finding: a scout book with the champion's signal but no six-tranche overlap scored validation Sharpe **1.166**, above the champion's own 1.120 — decomposition shows 89% of that gap is just extra breadth, not "fresher" information, so the overlap's validation-side justification was mis-attributed for ~20 trials. This does **not** license a challenger: the overlap's real justification is the holdout, which stays untouched by design. `portfolio-learning` closed entirely (all three combination operators refuted), while `seasonality-calendar` and `statistical-learning` both set new family-best leads (0.846 and 0.875).

## Champion

`mom_zscore_overlap6_hzn_avg4` — four-horizon (12-1/9-1/6-1/3-1) momentum, magnitude-weighted, six-tranche formation-date overlap (K=6), cohort vol-spike trim, hold-25/enter-15 membership buffer.

| Split | Sharpe | Ann. Return | Max DD | Turnover |
|---|---|---|---|---|
| Train | 0.97 | 18.6% | -57.2% | 2.0x |
| Validation | 1.12 | 25.6% | -27.8% | 3.1x |
| Holdout | **1.292** | 32.3% | -20.1% | 2.7x |

**Trend vs prior weeks:** identical to the last two reports — no trial reached the challenge track, so nothing about the seat itself has moved. What changed is the lab's understanding of *why* the seat looks the way it does on validation (see below).

## Promotions & retirements

None. Every trial this week ran on the scout track, so `PROMOTE`, `REJECT` and `HOLDOUT_VETO` were all unreachable outcomes.

## Trial activity

- **8 trials** this week (all scout track), down from last week's 27 — cumulative trial count **90** (up from 82). DSR deflator: **24 effective trials** at ρ=0.95 clustering (up from 23).
- Current deflated-Sharpe bar: **DSR ≥ 0.95** for promotion, now computed against 90 trials / 24 effective.
- Verdicts: 0 PROMOTE, 0 REJECT/HOLDOUT_VETO, **3 FAMILY_LEAD**, 5 SCOUT.
- New family leads this week: `price-trend` (as scout) 0.701 → **1.166**, `seasonality-calendar` 0.782 → **0.846**, `statistical-learning` 0.634 → **0.875**. `liquidity-volume` (0.942) and `portfolio-learning` (1.008) unchanged.
- No holdout was read by any session this week.

## Top 3 learnings

1. **The six-tranche overlap's validation-side justification was mis-attributed for ~20 trials — it buys breadth, not fresher information — but this changes nothing about the seat.** Two matched-breadth K=1 scouts (49.2 and 62.5 names, the latter pinned to the champion's own 62.69) decompose the champion's −0.109 validation gap to the best narrow challenger: **89% is the plain price of holding more names, 11% is the vintage-freshness residual, and that residual is statistically indistinguishable from zero** (fit R²=0.975 on Sharpe vs. book size alone). The lab is explicit that this is a **validation-only** statement: the human's 2026-08-26 rollback to K=6 was made on the *holdout* (K=6: 1.292; the four K=1-ward successors collapsed 1.377→0.691 there), and no trial this week read holdout. Nothing here licenses a K=1 challenger.
2. **`portfolio-learning` is closed on all three ways of combining two signals into one book.** Following up on the mean (bounded) and max (content-blind) operators closed earlier, this week's intersection operator was shown to respond to leg content (unlike max) but still can't beat its own better leg even after fixing a breadth-scaling confound — eleven trials, three operators, three failures. Recommendation: stop proposing combiners over these legs.
3. **A new pre-trial "depth profile" screen — each score's marginal excess by rank slice — passed its first two prospective tests.** It predicted, before either candidate was written, that narrowing `seasonality-calendar`'s inherited band would help (it did: 0.782→0.846) and that widening it would hurt (it did: 0.782→0.705) — the opposite sign to the `liquidity-volume` bracket the rule was originally fitted on. This is now a standard pre-registration step: profile the score, print a placebo alongside it, before choosing any band.

## Notable failures worth remembering

- **The parametric portfolio policy's headline claim collapsed for free, after a promising trial.** A weights-are-affine-in-characteristics construction (no ranking, no band) scored validation Sharpe **0.875** — the second-best non-`price-trend` result ever — but a same-night measurement showed its advertised "breadth chosen by the utility, not a band" is false: the long-only clip creates a hard arithmetic floor near half the pool that no rescaling can go below. A follow-up candidate built to pin breadth tighter was written, found to emit zero valid rows on validation, and deleted without being scored.
- **An excess screen predicted a gain from dropping ETFs out of the liquidity-volume book (+2.14pp/yr); the actual trial reversed sign.** The mechanism argument (ILLIQ mis-describes ETFs) still stands, but the score-level screen was blind to a variance channel the construction also moved — a reminder to check a book's volatility percentile alongside any tail-excess prediction.
- **A "kill switch" robustness test (Henriksson–Merton) gave a false green light on a mechanism the lab already refuted** (the 200-day trend-switch overlay). It's now used only to kill candidates, never to confirm them.
- **`range-variance` remains the only family with zero recorded trials, for the eleventh session running**, now backed by fifteen screened mechanisms and four independent statistics all pointing at the same survivorship-driven volatility artifact as the reason nothing there is tradeable. This still needs a human decision: amend `program.md`'s cold-family rule or formally retire the family.

## Operational note

The stray-branch issue flagged in the last two reports (sessions landing on throwaway `origin/main-*` branches) appears resolved: this week's sessions verified their per-run branch was bit-identical to `origin/main` before working and pushed to both; today's integrity check reports cleanly on `main` with no stray branches. Worth confirming it stays fixed next week before closing the item.

## Suggested focus for next week

1. **Still needs a human:** decide on `range-variance` — amend the cold-family allocation rule in `program.md` or retire the family slug. Eleven sessions running without a resolution.
2. The one live lever on the new `statistical-learning` lead is its characteristic set — adding a characteristic whose own depth profile carries past rank ~45 (only `liquidity-volume`'s ILLIQ score does so far) is the only way its arithmetic-floor breadth would become appropriate rather than merely unavoidable.
3. Continue scouting remaining families under the budget rules; do not propose a K=1 `price-trend` challenger on the strength of this week's decomposition — that reading is validation-only and the holdout question it would reopen is exactly the one the human's rollback already settled.

---
**Permanent caveat:** results are computed on free, survivorship-biased daily price data (today's constituents only, no point-in-time universe, no fundamentals/intraday) with costs modeled at 15 bps/side. This is research output for an autonomous experimentation system, not investment advice.
