# Weekly Report — 2026-W38 (Sep 13–19)

**TL;DR:** The champion is unchanged (`mom_zscore_overlap6_hzn_avg4`, holdout Sharpe 1.292) — no holdout look was spent this week, all 4 trials ran on the scout track. The week's real news is two flags for a human, not a new strategy: a self-audit found the DSR deflator counts 90+ trials as **24 independent bets when the return-series structure says 1.6–4.4**, which the lab explicitly did not file as a bug (code matches its "deliberately conservative" docstring) but says needs a calibration decision; and a mis-specified volatility call was found baked into a committed `strategies/lib/` file, left unfixed per the file-permission rule since editing it in place would silently change two recorded trials. On strategy, "blending beats switching" now covers *internal* state too — a sleeve-timing rule with a clean train screen (+1.55%/yr, t=+2.25) lost to its own always-on control on validation — and `range-variance` got its first trial (family lead, at the equal-weight floor) while `lead-lag-spillover` closed entirely.

## Champion

`mom_zscore_overlap6_hzn_avg4` — four-horizon (12-1/9-1/6-1/3-1) momentum, magnitude-weighted, six-tranche formation-date overlap (K=6), cohort vol-spike trim, hold-25/enter-15 membership buffer.

| Split | Sharpe | Ann. Return | Max DD | Turnover |
|---|---|---|---|---|
| Train | 0.97 | 18.6% | -57.2% | 2.0x |
| Validation | 1.12 | 25.6% | -27.8% | 3.1x |
| Holdout | **1.292** | 32.3% | -20.1% | 2.7x |

**Trend vs prior weeks:** identical for the fourth report running — no trial has reached the challenge track since the last holdout look (2026-08-17, five looks total to date). This week did add a measurement about the champion's own signal: a decomposition shows its entire out-of-performance over an equal-weight book is earned **overnight** (validation +16.36%/yr, t=+4.96) and it **loses** money intraday (−2.18%/yr) — not tradeable given this engine's close-to-close execution, but a concrete data point for why an intraday-bar upgrade would matter.

## Promotions & retirements

None. Every trial this week ran on the scout track, so `PROMOTE`, `REJECT` and `HOLDOUT_VETO` were all unreachable outcomes.

## Trial activity

- **4 trials** this week (all scout track), down from last week's 8 — cumulative trial count **94** (up from 90). DSR deflator: **25 effective trials** at ρ=0.95 clustering (up from 24).
- Current deflated-Sharpe bar: **DSR ≥ 0.95** for promotion, now computed against 94 trials / 25 effective.
- Verdicts: 0 PROMOTE, 0 REJECT/HOLDOUT_VETO, **1 FAMILY_LEAD**, 3 SCOUT.
- New family lead: `range-variance` opened its first-ever trial — `rv_volofvol_top15` at **0.494** (equal-weight floor). `portfolio-learning` (1.008) and `liquidity-volume` (0.942) both saw new scouts that stayed below their standing leads (0.867 and 0.822 respectively).
- Three sessions (Sep 14–16) ran 0–1 trials while the lab audited its own statistics rather than proposing books; two sessions (Sep 17–18) returned to searching and produced 3 of the week's 4 trials.
- No holdout was read by any session this week.

## Top 3 learnings

1. **"Blending beats switching" now covers timing on a component's *own* past return, not just external state variables.** A designed pair of twelve-sleeve books — identical except one drops a sleeve when its own trailing-12-month return is negative — showed the timed version winning cleanly on train (+1.55%/yr, t=+2.25, sign survives all six leave-one-out drops, matched hash placebo at 0%) but **losing** on validation (−0.112 Sharpe, t≈−1.6). This is the fourth consecutive instance of "clean train screen, no validation transfer" the lab has recorded, and the cleanest one yet. The always-on control (0.867 validation) is now the second-best non-`price-trend` result on the board.
2. **The DSR deflator's effective-trial count may not match this repo's actual search structure — flagged for a human, not treated as a bug.** Eigenvalue/participation-ratio analysis of the 90 stored validation return series puts the effective number of independent bets at **1.6 (raw) to 4.4 (with the long-only PC1 constraint removed)**, against the engine's clustering-based count of 24. The clustering threshold (`TRIAL_CLUSTER_RHO = 0.95`, in frozen `engine/protocol.py`) sits at the 91st percentile of the repo's own pairwise correlation distribution — the code matches its documented "deliberately conservative" intent, so this is explicitly not an engine issue, but the lab flags that a low effective-N reading also cuts the other way (the deflator likely *under*-corrects against the wider literature universe the hypotheses are drawn from) and recommends no bar change pending a human decision.
3. **A mis-specified `garman_klass_vol` call was found inside a committed `strategies/lib/` file and deliberately left unfixed.** `sleeve_book.py:41` passes arguments in the wrong order, silently binding `close = 21` (the window, as an integer) instead of the price panel; it still runs without error and only rank-correlates +0.06 with the correct calculation. It feeds two of the twelve sleeves behind last week's second-best non-`price-trend` result (0.867). The trial's own conclusion is unaffected (the error cancels in the paired timed-vs-untimed contrast), but `CLAUDE.md`'s rule against editing existing lib files binds exactly here — fixing it in place would silently change two already-recorded trials. A human may want to retire the file by name.

## Notable failures worth remembering

- **`lead-lag-spillover` is now closed in both halves.** The regional half's Lo–MacKinlay non-trading model was rejected on every partition of the universe this week — including two partitions where no timezone offset could even exist — worse than its own no-lag-rejection null. The surviving cross-region violations turn out to sit exactly behind the engine's 1-day execution lag, so they're the trading calendar, not an exploitable pattern.
- **`beta-minus` and coskewness both close on evidence, with no trial spent.** `beta-minus` correlates +0.60 with the plain volatility level already ruled a survivorship artifact and collapses to noise inside a volatility tercile — the tenth time that control has killed a candidate in this family. Coskewness came in wrong-signed in every subperiod and failed its own pre-registered directional falsifier.
- **`pl_factor_momentum_timed/untimed`** — see Top Learning #1; recorded as `SCOUT`/`SCOUT`, not a rejection of the family (`portfolio-learning`'s lead stays at 1.008).
- **A signed sort where both ends beat the pool turned out to be the survivorship-dispersion artifact again**, the ninth time that shape has shown up under a different name — this time in an overnight-sentiment reversal score whose "long" and "short" legs were both positive.

## Suggested focus for next week

1. **Needs a human:** two calibration/hygiene items surfaced this week rather than being resolved — the `TRIAL_CLUSTER_RHO` deflator question (learning #2 above) and whether to retire `strategies/lib/sleeve_book.py` (learning #3 above). Neither is urgent, both are recorded plainly so they don't get re-discovered.
2. **Needs a human, restated from prior weeks:** 9 of 10 program.md families are now closed or mapped with a standing lead. The lab flagged mid-week that several sessions in a row were producing only diagnostics about its own gate rather than new books — that pattern broke by week's end, but the underlying question stands: either the universe gains new data (fundamentals, point-in-time constituents, intraday bars — this week's overnight/intraday decomposition makes the intraday case concretely) or `program.md`'s agenda needs to say what happens once families run out.
3. Continue working the folder's carried-forward research items (`SUMMARY.md` #109's winner-to-runner-up discount, #82, #49) and do not re-derive the now-exhaustively-priced blend board or re-open any of the closed axes listed in this week's do-not-extend notes.

---
**Permanent caveat:** results are computed on free, survivorship-biased daily price data (today's constituents only, no point-in-time universe, no fundamentals/intraday) with costs modeled at 15 bps/side. This is research output for an autonomous experimentation system, not investment advice.
